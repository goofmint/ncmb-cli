#!/usr/bin/env node
var program = require('commander');

program
  .version('0.0.1')
  .command('add [args]', 'Add data to DataStore')
  .command('list [args]', 'Search from DataStore')
  .parse(process.argv);
